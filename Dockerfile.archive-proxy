FROM python:3.12-slim
WORKDIR /app
COPY archive_proxy.py /app/archive_proxy.py
RUN pip install --no-cache-dir requests
ENV OAM_ARCHIVE_PROXY_HOST=0.0.0.0
ENV OAM_ARCHIVE_PROXY_PORT=8080
EXPOSE 8080
CMD ["python", "archive_proxy.py"]
